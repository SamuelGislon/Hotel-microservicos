server:
  port: 8083

spring:
  application:
    name: ms-pagamentos

  datasource:
    url: jdbc:postgresql://localhost:5432/hosped_pagamentos
    username: pagamentos_user
    password: pagamentos_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mail:
    host: smtp.gmail.com
    port: 587
    username: hosped.hotel@gmail.com
    password: tcumibvrdqklywje
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
          auth-login-disable: true
        debug: true

pagamento:
  expiracao:
    horas: 8

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs