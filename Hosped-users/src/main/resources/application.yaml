spring:
  application:
    name: ms-users

  datasource:
    url: jdbc:postgresql://localhost:5432/hosped_users
    username: postgres
    password: 9973155
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8085

jwt:
  secret: hosped@Secret#2025!xK9mPqL3nZvB8wR
  expiration: 28800000