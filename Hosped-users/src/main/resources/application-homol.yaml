app:
  environment: ${APP_ENVIRONMENT:HOMOL}

spring:
  jpa:
    show-sql: false

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: WARN
    com.user.api_users: INFO

info:
  app:
    environment: ${APP_ENVIRONMENT:HOMOL}
