app:
  environment: ${APP_ENVIRONMENT:DEV}

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    br.edu.udesc.reservaservice: DEBUG

info:
  app:
    environment: ${APP_ENVIRONMENT:DEV}
